channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # External tools used by the pipeline. The -10/-35 scan reads MEME-format
  # files directly in Python, so the MEME suite is not required.
  - python=3.12
  - prokka
  - hmmer
  - pip
